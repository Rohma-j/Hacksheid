in week2 we buid a server,basic html form and a data file.
it helps understanding how server works and how all three of them are going to connect and work together.
the data entered in form is campared by database and decided whether it matches or not.
it follows the path from client request -> server respond by form -> user enter data -> data is recived by servar ->server respond accordingly
### updated week2
We updated the login form now we created the app using database and sql. In this we created the login form and directly using the input in sql queries. Furthermore we allow the user to access the profile by directly inserting id number in url and we are also using that directly into our queries.
- The database used in week2 is same as week3.